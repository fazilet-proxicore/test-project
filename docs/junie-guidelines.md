### Junie Guidelines — Merging Guidelines Guide

This guide describes how to use the `JunieGuidelines` module provided by the `proxicore/bedrock` package to merge multiple guideline files into a single master document.


